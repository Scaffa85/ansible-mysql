---
# Removes unused repository to prevent apt from getting packages from different versions.

- lineinfile:
    path: "{{ repository_path }}/{{ repo_mysql_com_apt_ubuntu }}"
    regexp: '5\.7'
    state: absent


